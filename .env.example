# PostgreSQL connection string. Keep the real value in .env.local (never commit it).
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require

# auto: PostgreSQL when available, otherwise static TypeScript data
# database: require PostgreSQL and surface configuration/query errors
# static: always use the generated TypeScript files
FANTASY_DATA_SOURCE=auto
